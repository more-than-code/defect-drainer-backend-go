.data/
bin/
dist/
# Root-anchored: an unanchored "defect-drainer" would ignore cmd/defect-drainer/.
/defect-drainer
/defect-drainer.exe
*.test
coverage.out
.env
.DS_Store
SESSION.md

# Live task ledger: never travels with a branch checkout (keeps it out of worker
# worktrees and merges).
tasks/

